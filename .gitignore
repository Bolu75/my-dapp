# --- SECURITY (CRITICAL) ---
# Prevents your private keys and API keys from leaking
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Note: We are NOT ignoring config.js anymore because 
# it now uses environment variables!

# --- DEPENDENCIES ---
# Ignores node_modules in both frontend and backend
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- HARDHAT (BACKEND) ---
backend/artifacts/
backend/cache/
backend/typechain/
backend/typechain-types/
backend/coverage/
backend/coverage.json
backend/.openzeppelin/
backend/ignition/deployments/

# --- VITE (FRONTEND) ---
frontend/dist/
frontend/dist-ssr/
frontend/*.local

# --- SYSTEM / IDE ---
.vscode/
.idea/
.DS_Store
Thumbs.db